#ifndef STATSH
#define STATSH
float calcularMediaRecursiva(float *leituras); /*calcula recursivamente a médias das leituras*/
float calcularMedia(float *leituras);//calcula normalmente a média das leituras
float achaMaior(float *leituras); //encontra o maior valor dentro das leituras
float achaMenor(float *leituras); //encontra o menor valor dentro das leituras
float calcularVariancia(float *leituras); //calcula a variancia das leituras
float calcularDesvioPadrao(float *leituras);//calcula o desvio padrão das leituras
#endif